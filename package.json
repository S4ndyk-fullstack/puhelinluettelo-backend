{
  "name": "puhelinluettelo-backend",
  "version": "1.0.0",
  "description": "Backend for puhelinluettelo",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "watch": "nodemon index.js",
    "build:ui": "rm -rf ./build && cd ../puhelinluettelo && npm run build --prod && cp -r build ../puhelinluettelo-backend"
  },
  "keywords": [
    "backend"
  ],
  "author": "Santtu Lempinen",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.3",
    "mongoose": "^5.7.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "nodemon": "^1.19.4"
  }
}
